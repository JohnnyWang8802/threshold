# 演示录制脚本

总长约 3 分 30 秒。左边终端，右边浏览器，比例 6:4。

---

## 00:00 – 00:12　冷开场

不解说。全屏播 `out/threshold.mp4` 的解码段（视频 0–6 秒），画面从噪声里长出弓箭手。

播到人脸刚成形时**暂停**，切终端。

> 第一句话再出：「这张图里每一个 0 和 1，都对应原图的一小格。」

---

## 00:12 – 00:35　看原图，说清难点

```bash
open input/source.png
```

指出两件事：整张片子极暗，中位亮度只有 90;主体（脸、弓弦）藏在阴影里，对比度极低。

> 「直接转成字符画，这些会全部消失。先演示一遍失败的样子。」

---

## 00:35 – 01:05　故意翻车一次

```bash
python3 src/mosaic.py --src input/source.png --out out/naive.png --no-clahe --no-ink-compensation
open out/naive.png
```

一片灰黑，什么都看不见。**这个镜头是整条片子的支点**，别跳过。

---

## 01:05 – 01:50　两处修复，逐个加回来

在 Claude Code 里打开 `src/mosaic.py`，定位到 `prep()`：

> 「第一处：CLAHE，在 LAB 的 L 通道上做局部提亮，色相不动。」

```bash
python3 src/mosaic.py --src input/source.png --out out/step1.png --no-ink-compensation
```

人物出来了，但整体还是偏灰。定位到 `ink` 那几行：

> 「第二处：0 和 1 的笔画只占格子 31%，不补偿，亮度就掉到原图的三分之一。」

```bash
make still
open out/still.png
```

**放大到 400%**，让每个字符清晰可辨。这是全片信息密度最高的一帧，停 5 秒。

---

## 01:50 – 02:20　从静态到动态

> 「静态图其实只是一帧。」

```bash
make web
open out/live.html
```

依次点四个模式，每个停约 6 秒。解码模式**手动拖速度滑杆到最慢**，停在解析中途，然后点「存下这一帧」。

> 「亮度分布是烘死的，动的只是每格显示 0 还是 1 —— 所以怎么翻转，图像都不会散。」

---

## 02:20 – 02:50　换一张图，证明是管线不是手工

拖一张完全不同的图进 `input/`：

```bash
make SRC=input/另一张.png
open out/still.png
```

不解说，让它自己跑完。

---

## 02:50 – 03:30　收尾

```bash
make video
```

ffmpeg 进度条跑一段（可 4 倍速）。最后全屏播完整 24 秒 MP4，不加字幕，直接黑场。

---

## 录制设置

| 项 | 值 |
|---|---|
| 分辨率 | 1920×1080，60fps |
| 终端字号 | 16pt 以上，深色主题 |
| 隐藏 | 通知、书签栏、桌面图标、真实文件路径 |
| 光标 | 开启点击高亮 |
| 配乐 | 无，或极低的环境底噪 |

## 剪辑提醒

- 所有 `pip install` / 长时间渲染，一律跳切或 8 倍速
- 每次运行命令后留足 1.5 秒再切下一镜，给观众读输出的时间
- 翻车镜头（00:35）和 400% 放大镜头（01:40）是全片仅有的两个必须完整保留的长镜
